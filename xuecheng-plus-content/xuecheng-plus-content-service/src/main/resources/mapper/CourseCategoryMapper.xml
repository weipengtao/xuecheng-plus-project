<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuecheng.content.mapper.CourseCategoryMapper">
    <select id="selectTreeNodes" resultType="com.xuecheng.content.model.dto.CourseCategoryTreeDTO">
        with recursive tree_node as (
            select * from course_category where id = #{id}
            union all
            select child.* from course_category child inner join tree_node on child.parentid = tree_node.id
        )
        select * from tree_node order by tree_node.id, tree_node.orderby
    </select>
</mapper>
